/// Application type enum
/// Determines which app variant to run
enum AppType {
  /// Client-facing application
  client,

  /// Admin/management application
  admin,

  /// Unified application (both client and admin routes)
  unified,
}
